# Installation

## Requirements

- Windows / Linux
- Rust stable toolchain
- No external dependencies

## Build

cargo build --release

## Run

cargo run --release

The engine listens on port 8080.

## Stop

Press Ctrl+C for a clean shutdown.
